import{_ as t,o as a,c as n,a3 as o}from"./chunks/framework.CfYLvWnO.js";const b=JSON.parse('{"title":"Renaming tables","description":"","frontmatter":{"order":30},"headers":[],"relativePath":"migrations/renaming-tables.md","filePath":"migrations/renaming-tables.md"}'),i={name:"migrations/renaming-tables.md"};function r(s,e,d,h,l,m){return a(),n("div",null,[...e[0]||(e[0]=[o('<h1 id="renaming-tables" tabindex="-1">Renaming tables <a class="header-anchor" href="#renaming-tables" aria-label="Permalink to &quot;Renaming tables&quot;">​</a></h1><p>Rename the main table with <code>ToTable(...)</code>, or the history table with <code>UseHistoryTable(...)</code> inside <code>IsTemporal(...)</code>, and the migration renames it in place with a <code>RenameTableOperation</code>. History keeps growing under the new name: nothing is split, and everything <code>AsOf</code>, <code>AllVersions</code> and <code>History&lt;T&gt;</code> could read before the rename they can still read after it.</p><table tabindex="0"><thead><tr><th>you rename</th><th>the migration renames</th><th>trigger writer</th></tr></thead><tbody><tr><td>the main table, history table at its default name</td><td>both tables, since the default history name follows the main table</td><td>drops the function and trigger under the old name and recreates them under the new one</td></tr><tr><td>the main table, history table named with <code>UseHistoryTable(...)</code></td><td>the main table only</td><td>nothing to do: PostgreSQL carries the trigger along with the table</td></tr><tr><td>the history table only</td><td>the history table only</td><td>drops and recreates the function and trigger, whose names and body contain the history table&#39;s name</td></tr></tbody></table><p>In both writer modes, the history table&#39;s period-range index is renamed along with the table. PostgreSQL&#39;s <code>ALTER TABLE … RENAME</code> does not rename indexes, and a stale name would later collide with the index of another entity whose history table happens to take the freed-up name. (The version index is an ordinary EF Core index, which EF Core renames itself.)</p><p>This works because a history table&#39;s identity in the model does not depend on its name. Upgrading Hindsight without renaming anything produces no migration changes.</p><h2 id="moving-a-table-to-a-different-schema" tabindex="-1">Moving a table to a different schema <a class="header-anchor" href="#moving-a-table-to-a-different-schema" aria-label="Permalink to &quot;Moving a table to a different schema&quot;">​</a></h2><p>Change the schema in <code>ToTable(&quot;name&quot;, &quot;schema&quot;)</code> or <code>UseHistoryTable(&quot;name&quot;, &quot;schema&quot;)</code>, and the migration moves it with <code>ALTER TABLE ... SET SCHEMA</code>. History keeps growing under the new schema, the same way a name-only rename keeps it growing under the new name.</p><p>PostgreSQL moves every object a table owns — its indexes included — into the new schema automatically when the table moves, so the period-range index needs no DDL of its own for this. The trigger function is a standalone object, unaffected by the table&#39;s own move, and is recreated under the new schema the same way it is for a name change.</p>',8)])])}const g=t(i,[["render",r]]);export{b as __pageData,g as default};
