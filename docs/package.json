{
  "name": "hindsight-docs",
  "private": true,
  "type": "module",
  "description": "Hindsight documentation site (VitePress). See docs/README.md.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "node scripts/check-snippets.mjs && vitepress build && node scripts/check-anchors.mjs",
    "preview": "vitepress preview",
    "check:snippets": "node scripts/check-snippets.mjs",
    "test": "node --test \"scripts/*.test.mjs\""
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
