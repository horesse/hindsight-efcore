import{_ as i,o as t,c as a,a3 as s}from"./chunks/framework.FagJqzbK.js";const p=JSON.parse('{"title":"Existing tables and upgrades","description":"","frontmatter":{"order":50},"headers":[],"relativePath":"migrations/existing-tables.md","filePath":"migrations/existing-tables.md"}'),n={name:"migrations/existing-tables.md"};function o(r,e,d,h,l,c){return t(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="existing-tables-and-upgrades" tabindex="-1">Existing tables and upgrades <a class="header-anchor" href="#existing-tables-and-upgrades" aria-label="Permalink to &quot;Existing tables and upgrades&quot;">​</a></h1><h2 id="making-an-existing-table-temporal" tabindex="-1">Making an existing table temporal <a class="header-anchor" href="#making-an-existing-table-temporal" aria-label="Permalink to &quot;Making an existing table temporal&quot;">​</a></h2><p>When you call <code>IsTemporal()</code> on an entity whose table already has rows, the migration creates the history table <strong>and seeds it automatically</strong>: right after <code>CREATE TABLE</code> (and, under the <a href="/hindsight-efcore/nightly/writing/history-writers">Trigger writer</a>, after the trigger), it appends</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INSERT INTO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> policies_history (id, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, premium, effective_from, valid_from, valid_to, operation)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SELECT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> id, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, premium, effective_from, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;infinity&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">FROM</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> policies;</span></span></code></pre></div><p>— one seeded version per existing row, using every versioned column (leaving out <a href="/hindsight-efcore/nightly/configuration/temporal-entities#excluding-properties">excluded</a> ones and a composite key&#39;s columns appear like any other). <code>valid_from = now()</code> means &quot;known since the migration&quot;: <code>AsOf</code> before that instant still correctly returns nothing. <code>operation = 1</code> records it as an insert, like any other first version. The change-context columns (<code>changed_by</code>, <code>changed_by_name</code>, <code>correlation_id</code>, <code>reason</code>, <code>extra</code>, and the opt-in <code>db_session_user</code>) stay <code>NULL</code> — there is no change context to attribute a migration-time seed to.</p><p>This runs unconditionally for every newly created history table, in both writer modes, whether the entity is brand new (its main table is created empty in the same migration, so the <code>SELECT</code> returns zero rows — a no-op) or, as here, an existing entity that just had <code>IsTemporal()</code> added. It runs exactly once, next to the table&#39;s own creation; a table already made temporal keeps growing its history through the ordinary writer path from then on, not through seeding again.</p><p><strong>You do not need to write this by hand anymore.</strong> If you want a different <code>valid_from</code> (e.g., &quot;these rows have really been valid since their <code>created_at</code>, not since the migration ran&quot;) or want to exclude a column differently than <a href="/hindsight-efcore/nightly/configuration/temporal-entities#excluding-properties"><code>Exclude(...)</code></a> already does, edit the generated migration&#39;s seeding statement, or write your own with <code>migrationBuilder.Sql(…)</code> right after the generated one and delete the generated one — the shape above is exactly what you&#39;d write by hand:</p><ul><li>List every versioned column, using your actual column names; leave out excluded ones.</li><li><code>valid_from</code> is the instant you want <code>AsOf</code> to start returning the row; <code>valid_to = &#39;infinity&#39;</code> marks it current.</li><li><code>operation = 1</code> records it as an insert. Leave the change-context columns <code>NULL</code> unless you have a real value to attribute the seed to.</li></ul><p>If you&#39;re upgrading from a version of Hindsight that predates this (before the release that shipped DESIGN.md D6&#39;s &quot;existing non-empty table made temporal&quot; bullet) and already hand-wrote this seeding SQL per the guidance above, see <a href="/hindsight-efcore/nightly/reference/upgrading">Upgrading Hindsight</a> — your next such migration would otherwise seed the same rows twice.</p><h2 id="upgrading-hindsight" tabindex="-1">Upgrading Hindsight <a class="header-anchor" href="#upgrading-hindsight" aria-label="Permalink to &quot;Upgrading Hindsight&quot;">​</a></h2><p>Upgrading the package is normally a no-op for your schema: run <code>dotnet ef migrations add</code> and expect an empty migration. When a release needs something more, the <a href="/hindsight-efcore/nightly/reference/upgrading">upgrade guide</a> says so, version by version.</p><h3 id="backfilling-the-period-range-index" tabindex="-1">Backfilling the period-range index <a class="header-anchor" href="#backfilling-the-period-range-index" aria-label="Permalink to &quot;Backfilling the period-range index {#backfilling-the-period-range-index}&quot;">​</a></h3><p>Since 1.1, every new history table gets a GiST index on its period, <code>ix_&lt;history_table&gt;_period</code> (see <a href="/hindsight-efcore/nightly/migrations/generated-schema#indexes">What a migration creates</a>). History tables created by 1.0 do not have it, and upgrading does not add it: the index is created only next to the history table&#39;s <code>CREATE TABLE</code>, so for existing tables <code>dotnet ef migrations add</code> scaffolds nothing.</p><p>Without it, a point-in-time query that is not also filtered by the entity&#39;s key scans the whole history table. Add it yourself, in a hand-written migration or directly:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CONCURRENTLY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> IF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> EXISTS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ix_policies_history_period</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> policies_history </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">USING</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> gist (tstzrange(valid_from, valid_to));</span></span></code></pre></div><ul><li>Use <code>CONCURRENTLY</code>. A plain <code>CREATE INDEX</code> blocks writes to the history table, and so every write to the main table under the trigger writer, for as long as the build takes. <code>CONCURRENTLY</code> cannot run inside a transaction, so run it outside a migration transaction.</li><li>Substitute your history table&#39;s name, and your period column names if you changed them with <code>HasPeriodStart</code> / <code>HasPeriodEnd</code>.</li><li>Keep the name <code>ix_&lt;history_table&gt;_period</code>, so your schema matches what a freshly created table gets.</li></ul>`,16)])])}const k=i(n,[["render",o]]);export{p as __pageData,k as default};
