import{_ as t,o,c as r,a3 as a}from"./chunks/framework.FagJqzbK.js";const c=JSON.parse('{"title":"Evolving a temporal entity","description":"","frontmatter":{"order":20},"headers":[],"relativePath":"migrations/schema-evolution.md","filePath":"migrations/schema-evolution.md"}'),n={name:"migrations/schema-evolution.md"};function i(d,e,s,l,h,p){return o(),r("div",null,[...e[0]||(e[0]=[a('<h1 id="evolving-a-temporal-entity" tabindex="-1">Evolving a temporal entity <a class="header-anchor" href="#evolving-a-temporal-entity" aria-label="Permalink to &quot;Evolving a temporal entity&quot;">​</a></h1><p>History tables are ordinary entity types in the EF Core model, so <code>dotnet ef migrations add</code> sees them and generates their DDL along with the main table&#39;s. One rule decides <em>what</em> it generates: <strong>a generated migration never destroys history.</strong></p><table tabindex="0"><thead><tr><th>you change the entity</th><th>main table</th><th>history table</th></tr></thead><tbody><tr><td>add a property</td><td>column added</td><td>column added; existing versions get <code>NULL</code></td></tr><tr><td>remove a property</td><td>column dropped</td><td>column <strong>kept</strong>, nullable</td></tr><tr><td>rename a property</td><td>column renamed</td><td>new column added; the old one is <strong>kept</strong>, nullable</td></tr><tr><td>change a property&#39;s type, precision/scale, max length or converter (same column name)</td><td>—</td><td><strong>rejected</strong></td></tr><tr><td>remove a primary-key property</td><td>—</td><td><strong>rejected</strong></td></tr></tbody></table><p>The columns of <a href="/hindsight-efcore/nightly/configuration/nested-members">complex properties and owned references</a> follow the same rules: a member added, removed or renamed is a set of columns added, removed or renamed.</p><h2 id="adding-a-property" tabindex="-1">Adding a property <a class="header-anchor" href="#adding-a-property" aria-label="Permalink to &quot;Adding a property&quot;">​</a></h2><p>Add the property to the entity. The migration adds the column to both tables. Existing history rows get <code>NULL</code>, or the column&#39;s default if it has one.</p><h2 id="removing-a-property" tabindex="-1">Removing a property <a class="header-anchor" href="#removing-a-property" aria-label="Permalink to &quot;Removing a property&quot;">​</a></h2><p>Remove the property from the entity. The migration drops the column from the main table and <strong>keeps it in the history table</strong>, made nullable. Old versions still hold data in it, and dropping the column would destroy the history this library exists to keep.</p><p>Hindsight does this by keeping the column in the model: on every model build it reads the previous model snapshot and re-adds each history column that has lost its property, tagged <code>Hindsight:Orphaned</code>. The migrations differ therefore never sees a column to drop. A model build with no other changes scaffolds an empty migration.</p><p>If you really want a history column gone, write the migration that drops it yourself. Hindsight never will.</p><h2 id="renaming-a-property" tabindex="-1">Renaming a property <a class="header-anchor" href="#renaming-a-property" aria-label="Permalink to &quot;Renaming a property&quot;">​</a></h2><p>EF Core&#39;s rename detection applies to the main table. In the history table a rename becomes <em>remove plus add</em>: the new column appears, and the old one stays as a nullable orphan with its data.</p><p>This is deliberate. In history, a rename cannot be told apart from a change of meaning, and guessing wrong would silently rewrite old versions.</p><h2 id="changing-a-property-s-type" tabindex="-1">Changing a property&#39;s type <a class="header-anchor" href="#changing-a-property-s-type" aria-label="Permalink to &quot;Changing a property&#39;s type&quot;">​</a></h2><p>Rejected with <code>InvalidOperationException</code> when the model is built, for a property whose store type, precision/scale, max length or value converter changed while its column stayed under the same name.</p><p>The history table mirrors a live column&#39;s type onto the same column name, so altering it in place would touch the history table too — and unlike removing or renaming a property, there is no &quot;keep the old one&quot; fallback here, because the column never leaves the model: the change would be a genuine <code>ALTER COLUMN</code> against rows that may hold values only valid under the old type (<code>text</code> history rows that are not valid JSON, altered to <code>jsonb</code>, are a concrete way this fails outright).</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Give the property a different column name instead. The old column becomes a nullable orphan — the same outcome as <a href="#renaming-a-property">renaming a property</a> — and the new one starts clean under the new type. If the physical column really must change type in place, write that migration by hand; Hindsight never will.</p></div><h2 id="removing-a-primary-key-property" tabindex="-1">Removing a primary-key property <a class="header-anchor" href="#removing-a-primary-key-property" aria-label="Permalink to &quot;Removing a primary-key property&quot;">​</a></h2><p>Rejected with <code>InvalidOperationException</code> when the model is built. The history table&#39;s version index and the writer&#39;s &quot;close the previous version&quot; step are keyed on the primary key. Keep the property, or <a href="/hindsight-efcore/nightly/migrations/removing-temporal">stop versioning the entity</a>.</p><h2 id="with-the-trigger-writer" tabindex="-1">With the trigger writer <a class="header-anchor" href="#with-the-trigger-writer" aria-label="Permalink to &quot;With the trigger writer&quot;">​</a></h2><p>Any migration that adds, removes or renames a column of a temporal entity also replaces the trigger function (<code>CREATE OR REPLACE FUNCTION</code>), so it always writes the current set of columns:</p><ul><li><strong>added column</strong>: the new function copies it into every new version;</li><li><strong>removed or renamed column</strong>: the old column stays in the history table and the new function stops writing it. Old versions keep their values; new versions get <code>NULL</code> there.</li></ul><p>Only the function body changes; the trigger definition and the history table are never dropped.</p>',23)])])}const g=t(n,[["render",i]]);export{c as __pageData,g as default};
