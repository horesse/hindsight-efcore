import{_ as t,o as i,c as o,a3 as r}from"./chunks/framework.FagJqzbK.js";const u=JSON.parse('{"title":"Upgrade guide","description":"","frontmatter":{"order":30,"sidebarTitle":"Upgrade guide"},"headers":[],"relativePath":"reference/upgrading.md","filePath":"reference/upgrading.md"}'),a={name:"reference/upgrading.md"};function n(s,e,d,h,l,c){return i(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="upgrade-guide" tabindex="-1">Upgrade guide <a class="header-anchor" href="#upgrade-guide" aria-label="Permalink to &quot;Upgrade guide&quot;">​</a></h1><p>What to do, and what to expect, when you move to a new version. For the complete list of changes in a release, see the <a href="https://github.com/horesse/hindsight-efcore/releases" target="_blank" rel="noreferrer">release notes</a>.</p><p>Hindsight follows semantic versioning: a minor or patch release never needs a code change on your side, but it may add a manual database step (listed here) or start rejecting something that was silently wrong before.</p><h2 id="upgrading-to-1-1" tabindex="-1">Upgrading to 1.1 <a class="header-anchor" href="#upgrading-to-1-1" aria-label="Permalink to &quot;Upgrading to 1.1&quot;">​</a></h2><h3 id="add-the-period-range-index-to-existing-history-tables" tabindex="-1">Add the period-range index to existing history tables <a class="header-anchor" href="#add-the-period-range-index-to-existing-history-tables" aria-label="Permalink to &quot;Add the period-range index to existing history tables&quot;">​</a></h3><p>History tables created by 1.1 get a GiST index on their period; tables created by 1.0 do not, and upgrading does not add it. Without it, point-in-time queries that are not filtered by key scan the whole history table. See <a href="/hindsight-efcore/nightly/migrations/existing-tables#backfilling-the-period-range-index">Backfilling the period-range index</a> for the SQL.</p><h3 id="configurations-that-now-fail-when-the-model-is-built" tabindex="-1">Configurations that now fail when the model is built <a class="header-anchor" href="#configurations-that-now-fail-when-the-model-is-built" aria-label="Permalink to &quot;Configurations that now fail when the model is built&quot;">​</a></h3><p>Each of these used to produce a history table that silently missed changes or corrupted history. They now throw at model build, which includes <code>dotnet ef migrations add</code>:</p><ul><li>every primary-key property excluded with <code>Exclude(...)</code>;</li><li>a column named like one of the history table&#39;s own columns, or like the period columns;</li><li>the same name for the period start and end columns;</li><li>a generated table, index, function or trigger name longer than 63 bytes;</li><li>an owned reference or a complex property on a temporal entity.</li></ul><p>See <a href="/hindsight-efcore/nightly/configuration/model-validation">Model validation</a> for the fixes.</p><h3 id="queries-that-now-throw" tabindex="-1">Queries that now throw <a class="header-anchor" href="#queries-that-now-throw" aria-label="Permalink to &quot;Queries that now throw&quot;">​</a></h3><ul><li><code>ExecuteUpdate</code> and <code>ExecuteDelete</code> on <code>AsOf</code>, <code>AllVersions</code> or <code>History&lt;T&gt;</code> throw <code>NotSupportedException</code>. <code>ExecuteUpdate</code> used to update the history table itself.</li><li><code>UseHindsight()</code> with a provider other than Npgsql throws on first use.</li></ul><h3 id="behavior-changes" tabindex="-1">Behavior changes <a class="header-anchor" href="#behavior-changes" aria-label="Permalink to &quot;Behavior changes&quot;">​</a></h3><ul><li><strong>Ambient <code>TransactionScope</code> works</strong> with both writers, instead of failing with &quot;an ambient transaction has been detected&quot;. See <a href="/hindsight-efcore/nightly/writing/transactions#transactionscope">Transactions</a>.</li><li><strong><code>EnableRetryOnFailure()</code> fails early</strong> with a Hindsight message naming the fix, when a save would need Hindsight&#39;s own transaction. See <a href="/hindsight-efcore/nightly/writing/transactions#enableretryonfailure">Transactions</a>.</li><li><strong><code>WithReason</code> applies to one <code>DbContext</code></strong>: a save on another context inside the same <code>using</code> block no longer picks up its reason.</li><li><strong>Interceptor writer, deletes</strong>: the row is re-read before the tombstone is written, one extra round trip per save that deletes. A delete of a row that no longer exists throws instead of writing an empty tombstone.</li><li><strong>Interceptor writer, failures</strong>: after a failed history write, <code>Added</code> and <code>Modified</code> entities are restored so that a retry works. See <a href="/hindsight-efcore/nightly/writing/interceptor#if-the-history-write-fails">If the history write fails</a>.</li><li><strong>Interceptor writer, concurrency</strong>: concurrent updates of one row no longer produce overlapping periods; the previous version is closed with the same clamp the trigger writer uses.</li></ul><h2 id="choosing-the-writer-explicitly" tabindex="-1">Choosing the writer explicitly <a class="header-anchor" href="#choosing-the-writer-explicitly" aria-label="Permalink to &quot;Choosing the writer explicitly&quot;">​</a></h2><p>Whether <code>HistoryWriter.Trigger</code> should become the default in a future major version is an open question. Whichever way it is decided, a context that calls <code>UseHistoryWriter(...)</code> explicitly keeps its behavior across upgrades, so name the writer you use.</p>',16)])])}const p=t(a,[["render",n]]);export{u as __pageData,p as default};
